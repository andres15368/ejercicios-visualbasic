Module Module1
    Dim num As Integer
    Dim a As Decimal
    Sub Main()
        Console.Write("ingrese numero")
        num = Console.ReadLine()
        a = num ^ (1 / 2)
        Console.WriteLine("la raiz cubica es: " & a)
        Console.ReadLine()

    End Sub

End Module
